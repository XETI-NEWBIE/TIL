
# [1] 목적구 설정 로직

white_x = balls[0][0]
white_y = balls[0][1]

target_indices = [1,3,5]
target_idx = -1

for idx in target_indices:
    if balls[idx][0]!=-1:
        target_idx=idx
        break

target_x = balls[target_idx][0]
target_y = balls[target_idx][1]

# [2] 가장 가까운 홀 찾기
best_hole = HOLES[0]
min_dist = 9999
for hole in HOLES:
    dist = math.sqrt((hole[0]-target_x)**2 + (hole[1]-target_y)**2) 
    if dist<min_dist:
        min_dist=dist
        best_hole=hole

# [3] 목적구 뒤 '접점' 좌표 구하기
angle_to_hole = math.atan2(best_hole[1]-target_y, best_hole[0]-target_x)
main_target_x = target_x - 5.73*math.cos(angle_to_hole)
main_target_y = target_y - 5.73*math.sin(angle_to_hole)

#쿠션로직
#angle_to_hole = math.atan2(best_hole[1]-target_y, best_hole[0]-target_x)
#contact_x = target_x - 5.73*math.cos(angle_to_hole)
#contact_y = target_y - 5.73*math.sin(angle_to_hole)
#main_target_x = contact_x
#main_target_y = -contact_y

# [4] 내 흰 공에서 접점까지의 최종 각도
dx = main_target_x - white_x
dy = main_target_y - white_y

angle = math.degrees(math.atan2(dy,dx))
if angle<0:
    angle+=360
power = 100.0

print({target_idx}, {angle}, {power})



'''
🎱 일타싸피 핵심 로직 4단계 요약

1단계: "누구를 칠 것인가?" (목적구 선정)

먼저 내 흰 공의 위치를 파악하고, 아직 테이블 위에 남아있는(구멍에 안 들어간) 공을 우선순위에 따라 고릅니다, 1.3 Python으로 적용해보기].

코드 흐름: balls[0]에서 내 위치를 가져오고, 반복문으로 balls[idx][0]이 -1이 아닌 첫 번째 공을 찾습니다.

핵심 포인트: target_idx가 결정되면 그 공의 좌표를 target_x, target_y에 저장합니다].

2단계: "어디로 넣을 것인가?" (최적의 홀 탐색)
당구대 위 6개의 구멍 중 목적구와 가장 가까운 구멍을 찾습니다. 거리가 짧을수록 정확도가 올라가기 때문입니다.

코드 흐름: 6개의 HOLES를 하나씩 대조하며 피타고라스 정리(math.sqrt)로 거리를 잽니다.

핵심 포인트: if dist < min_dist를 통해 가장 짧은 기록을 가진 구멍(best_hole)을 최종 선발합니다.

3단계: "옆구리 어디를 때릴 것인가?" (접점 계산)목적구의 정중앙을 치면 공이 엉뚱한 곳으로 가기 때문에, 구멍 반대편에 가상의 공(Ghost Ball)이 있다고 가정하고 그 지점($5.73\text{cm}$ 뒤)을 조준합니다.
코드 흐름: atan2로 목적구에서 홀까지의 각도를 구합니다.
cos와 sin을 이용해 그 각도의 **반대 방향($-$)**으로 $5.73\text{cm}$ 떨어진 좌표(main_target)를 구합니다.
쿠션 전략: 만약 앞에 장애물이 있다면, 접점 계산 대신 목적구의 $y$좌표를 거울처럼 반전(-target_y)시켜 벽을 맞히는 전략을 씁니다.

4단계: "얼마나 세게 쏠 것인가?" (최종 각도 및 파워)

이제 내 흰 공에서 앞서 구한 '가상의 목표 지점'을 향해 쏠 각도를 계산합니다 가이드].

코드 흐름: math.atan2(dy, dx)로 각도를 구하고, 서버가 이해할 수 있도록 math.degrees로 변환합니다.

핵심 포인트: 음수 각도는 +360을 더해 보정하고, 파워는 채원님의 전략대로 100.0 풀파워로 설정합니다
'''

































