# my_app/urls.py
from django.urls import path
from . import views

app_name = 'articles' # 또는 요구사항에 맞는 이름

urlpatterns = [
    # 여기에 앞으로 만들 기능들의 경로를 적어줄 거예요!
    # 예: path('', views.index, name='index'),
]